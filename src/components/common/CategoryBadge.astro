---
const categories: Record<string, string> = {
  general: '一般公告',
  event: '活动消息',
  competition: '比赛信息',
  academic: '学术分享',
  other: '其他',
};

const colors: Record<string, string> = {
  general: 'bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-300',
  event: 'bg-ocean-100 text-ocean-700 dark:bg-ocean-900/50 dark:text-ocean-300',
  competition: 'bg-orange-100 text-orange-700 dark:bg-orange-900/50 dark:text-orange-300',
  academic: 'bg-wave-100 text-wave-700 dark:bg-wave-900/50 dark:text-wave-300',
  other: 'bg-gray-100 text-gray-600 dark:bg-gray-800 dark:text-gray-400',
};

interface Props {
  category: string;
}

const { category } = Astro.props;
const label = categories[category] || category;
const colorClass = colors[category] || colors.other;
---

<span class={`inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium ${colorClass}`}>
  {label}
</span>
